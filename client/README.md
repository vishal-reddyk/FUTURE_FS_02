Client README

- Start: `npm run dev`
- Default env: Copy root `.env.example` to client/.env and set `VITE_API_URL`
- Pages:
  - /login
  - /app

This includes a minimal dashboard, login, and lead table to get started.
